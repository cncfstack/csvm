name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
        # git clone -b v1.38.1 https://github.com/kubernetes/minikube.git minikube
        # cd minikube/deploy/kicbase

        # # 1. not build auto-pause, and not need
        # # 2. '"$(which systemd)"' get null, abs path /usr/lib/systemd/systemd
        # # 3. CHANGELOG": not found
        # sed -i  -e '/auto-pause/d' \
        #         -e 's#"$(which systemd)"#/usr/lib/systemd/systemd#g' \
        #         -e '/CHANGELOG/d' \
        #         Dockerfile
        # cat Dockerfile
        docker login  --username=${{ secrets.HARBOR_PUSH_USERNAME }} --password=${{ secrets.HARBOR_PUSH_PASSWORD }} harbor.cncfstack.com
        docker buildx create --name mybuilder --use
        #docker buildx build --platform linux/amd64,linux/arm64  --build-arg KICBASE_IMAGE=node:22.21.1-bookworm-slim  --push . --file Dockerfile --tag harbor.cncfstack.com/cncfstack/csvm:dev
        docker buildx build --platform linux/amd64,linux/arm64  --push . --file Dockerfile --tag harbor.cncfstack.com/cncfstack/csvm:dev2
